{{#partial "principal" }}
<div class="w-100 py-5 my-5 d-flex justify-content-center align-items-center">
    <div class="rounded p-3 bg-body-tertiary">
        <strong class="w-100 mb-1 text-center">No existe un {{entity}} con ese ID</strong>
    </div>
    <form class="mt-3 w-100 d-flex justify-content-center align-items-center" action="/">
        <input type="submit" class="btn btn-primary" value="Home">
    </form>
</div>
{{/partial}}

{{> head.html.hbs }}
{{> layout.html.hbs }}
